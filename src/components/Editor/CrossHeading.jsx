import React from 'react';

export const CrossHeading = ({ node, children }) => {
    return (
        <div className="mt-4 mb-2 font-serif font-bold text-black">
            {children}
        </div>
    );
};
